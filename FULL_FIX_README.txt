MOJO Drive MVP 0.12 - FULL FIXED PATCH

This is the complete 0.12 patch bundle, not the tiny one-file build fix.
It includes the full 0.12 source changes plus the AndroidX build correction:
android.useAndroidX=true

Apply this ZIP over the current MOJODrive repository root, then commit and push.
